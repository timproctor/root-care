<header>
  <h2>Driver Trips</h2>
</header>
<table>
    <tr>
      <th>Driver's Name</th>
      <th>Total Distance</th>
      <th>Average Speed</th>
    </tr>
    <% drivers.each do |driver| %>
    <tr></tr>
    <% discard_5_100_mph_for(driver) %>
    <tr>
      <td><%= show_name_of(driver) %></td>
      <td><%= show_total_trip_miles_for(driver) %></td>
      <td>future numbers</td>
    </tr>
  <% end %>
</table>
